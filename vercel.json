{
  "version": 2,
  "name": "picassonova",
  "builds": [
    {
      "src": "apps/files/src/main.ts",
      "use": "@vercel/node",
      "config": { "distDir": "dist/apps/files" }
    },
    {
      "src": "apps/gateway/src/main.ts",
      "use": "@vercel/node",
      "config": { "distDir": "dist/apps/gateway" }
    }
  ],
  "routes": [
    {
      "src": "/api/v1/files/(.*)",
      "dest": "apps/files/src/main.ts",
      "methods": ["GET", "POST", "PUT", "DELETE", "PATCH"]
    },
    {
      "src": "/api/v1/gateway/(.*)",
      "dest": "apps/gateway/src/main.ts",
      "methods": ["GET", "POST", "PUT", "DELETE", "PATCH"]
    }
  ]
}
